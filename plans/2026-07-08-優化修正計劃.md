# stock-dashboard 優化修正計劃

> 建立：2026-07-08｜狀態：進行中
> **本文件的讀者是接續開發的 AI session**，不是人類。目標：讀完本文件即可知道未完成事項並接續開發。

---

## 0. Session 接續指引（每個新 session 先讀這裡）

### 0.1 開工前必讀

1. 本文件全文（尤其 §4 進度總覽 + 你要做的任務的完整條目）
2. `AGENTS.md`（Next.js 版本注意事項）
3. `docs/抓取檔案時序.md`（資料源與時序的權威對照表）
4. 動泡泡圖前：`Technical Discussion/對數刻度-Symlog-Scale.html`、`泡泡碰撞-Collision-Resolution.html`
5. 動 pipeline 前：`Technical Discussion/競態條件-Race-Condition-Lost-Update.html`、`不可變快照-Immutable-Snapshot-Delta.html`、`HTTP快取-ETag-Stale-While-Revalidate.html`、`事件驅動-Event-Driven-vs-Polling.html`

### 0.2 回寫規則（強制）

- 開始做任務時：把 §4 該任務狀態改為 `in-progress`
- 完成任務時：狀態改 `done`，並在 §5 執行紀錄 append 一行：`日期｜任務ID｜做了什麼｜驗證方式與結果`
- 被卡住時：狀態改 `blocked`，在 §5 記錄卡住原因
- 發現計劃與現實不符（檔案已改、API 變了）：以現實為準，修訂任務內容並在 §5 註明
- **不要跳過回寫**。這份文件是跨 session 的唯一交接棒

### 0.3 硬性約束（做任何任務都要遵守）

| 約束 | 內容 |
|---|---|
| Supabase 免費額度 | egress ~5GB/月、Storage 1GB。**任何改動不得增加「每次 run 下載的位元組數」**，新增大檔案前先算流量 |
| GitHub Actions | 若 repo 為 private，分鐘數 2000/月全帳號共用（Franky 有多個專案）。cron 班次總數不可超過 8/天；被 Guard 攔截的 run 必須在 1 分鐘內結束 |
| TWSE/TPEX | 非官方 API，請求間隔 ≥ 1.5 秒，單 run 請求數不要超過現況一個量級 |
| AI 相關功能 | 用 **OpenAI API**（Franky 有額度），不用 Claude API。例外：概念股分類的初版與擴充由 AI session 內分析完成，不呼叫任何外部 LLM API |
| 顏色慣例 | 正向/看多 = 紅、負向/看空 = 綠（台股慣例） |
| 時區 | 一律 Asia/Taipei；跨市場功能要用 per-market 時區設定，不可寫死 |
| 修改 workflow yml | 改完後提醒 Franky：GitHub Actions 排程改動要 push 到 main 才生效 |

---

## 1. 背景與決策記錄

### 1.1 專案現況（2026-07-08 分析結論）

- 三頁面：`/aftermarket`（主頁，四 Tab）、`/signals`、`/intraday`（placeholder）
- 資料流：n8n Phase1（籌碼）→ Supabase `chips/{date}.json` + dispatch → GitHub Actions `daily-fetch.yml`（7 cron/天）→ `fetch-daily.mjs`（讀舊快照+抓 TWSE/TPEX/FinMind）→ `calc-signals.mjs` → `write-firebase.mjs` → Supabase `latest.json` + `ohlc.json` + Firestore signals
- 上游專案：`../stock-chips-daily/`（Python + n8n），透過 `chips/{date}.json` 解耦
- 已確認兩個 Bug（見 P0-1、P0-2）
- `latest.json` 內嵌 1977 支 × 250 天 closes+dates，估 10MB 級；pipeline 每 run 自己下載一次 → 每月 2GB+ egress，逼近免費額度 → **分檔瘦身是配額生存問題，不是單純優化**

### 1.2 Franky 已確認的決策（2026-07-08 對話）

1. chips 欄位契約：正本放 `../stock-chips-daily/doc/chips-contract.md`（生產方持有），兩專案 CLAUDE.md/AGENTS.md 各加指引行
2. cron 班次「時段重分配」不增班（配額考量），且必須與 meta.json 瘦身綁定執行
3. `concept-sectors.json` 放本專案 `data/`，進 git；**初版由 AI session 內分析（不用 OpenAI）**；月度維護用 OpenAI + 人工確認 diff
4. AI 盤後總結等功能用 OpenAI API（不用 Claude API）
5. 泡泡圖對標 Tide-tw.app：單位改億元、symlog 軸、四象限統計條、排行榜、概念股細分（目標 Tide 級 100+ 概念）、個股概念 tags（一股多概念）
6. 全球指數（美四大+日經+KOSPI）：首頁純文字燈號（「XX 指數位在 N 日線以下」），點開 Modal 看 K 線 + 高低點標示 + MA 10/20/60/120 + 日/週線切換；台股大盤 K 線同步補 MA 與日/週切換
7. 資料鮮度：統一以「今日」為基準，未更新區塊顯示「更新中」；假日顯示最近交易日；個股拆 twseDate/tpexDate 兩個戳
8. 盤中即時（Fugle WebSocket）→ **Deferred**，最後才做
9. 歷史資料：衍生檔維持 250 天；新增每日 Delta 歸檔（`daily/{date}.json`，只存增量 ~200KB/天）可無限累積

### 1.3 目標資料架構（改造後）

```
寫入（GitHub Actions pipeline）：
  抓當日資料 → daily/{date}.json（Delta，不可變，immutable cache）
             → 疊加產生衍生檔：
                meta.json         (~1KB   Guard 用：各資料塊日期戳)
                market.json       (~100KB 大盤K線250d+籌碼+訊號+sectors+全球指數)
                stocks-lite.json  (~300KB 個股清單，無歷史陣列)
                ohlc.json         (現有，個股歷史 lazy load，closes 也移入)
             → latest.json（過渡期保留，全部任務完成後淘汰）

讀取（前端）：
  首頁載 market.json + stocks-lite.json；個股詳情 lazy 載 ohlc.json
  「看前一日」載 daily/{date}.json
```

---

## 2. 任務總覽與依賴關係

```
P0-1, P0-2（獨立，隨時可做）
P0-3 meta.json + Guard 前置 ──→ P1-1 檔案分層 ──→ P1-2 cron 重排
P1-3 億元單位 ──→ P1-4 symlog 軸 ──→ P1-5 統計條+排行榜
P2-1 概念分類資料（seed 已建）──→ P2-2 概念 tags UI ──→ P2-5 概念泡泡圖切換
P2-3 全球指數（獨立）
P1-6 資料鮮度（依賴 P0-3 的 meta.json 欄位）
P2-4 AI 盤後總結（依賴 P1-1 完成後的穩定管線）
P0-4 chips-contract.md（獨立，隨時可做）
```

---

## 3. 任務詳細條目

### P0-1｜修 Bug：concurrency group 無效 `[done]`

- **背景**：`.github/workflows/daily-fetch.yml` 的 `group: daily-fetch-${{ github.run_id }}` 用了 run_id，每 run 自成一組，併發保護形同虛設 → 兩 run 同時讀改寫 latest.json 會 lost update（融資被蓋掉的偶發問題根源之一）
- **改動檔案**：`.github/workflows/daily-fetch.yml`
- **做法**：`group` 改為固定字串 `daily-fetch`，`cancel-in-progress: false` 保留（後到的排隊不取消）
- **AC**：手動連續觸發兩次 workflow_dispatch，第二個 run 在 Actions UI 顯示 pending 排隊直到第一個結束
- **注意**：同時更新 yml 內註解（原註解「後進的直接取消」描述錯誤）

### P0-2｜修 Bug：buildChipsEntry null crash `[done]`

- **背景**：`scripts/fetch-daily.mjs` 的 `buildChipsEntry()` 中 `pcr: phase1.pcr` 與 `vix: phase1.vix` 少了 `?.`。設計上允許 phase1=null（n8n 未寫入但融資已就緒）建 partial entry，此時直接 TypeError 整個 run 失敗
- **改動檔案**：`scripts/fetch-daily.mjs`（搜尋 `phase1.pcr`）
- **做法**：改為 `phase1?.pcr ?? null`、`phase1?.vix ?? null`
- **AC**：`SUPABASE_URL=... node -e` 單測 `buildChipsEntry(null, 123.45, 120)` 不丟例外且回傳物件 `margin_amount === 123.45`、`pcr === null`

### P0-3｜Guard 前置 + meta.json `[done]`

- **背景**：Guard 1 目前下載整包 latest.json（~10MB）只為看幾個日期戳；且 workflow 先跑 npm ci 才執行 Guard。被攔截的 run 白耗 2 分鐘 + 10MB egress × 每天多班 × 每月 → 配額主要浪費源
- **改動檔案**：`scripts/fetch-daily.mjs`、`scripts/write-firebase.mjs`、`.github/workflows/daily-fetch.yml`
- **做法**：
  1. `write-firebase.mjs` 額外上傳 `meta.json`：`{ updatedAt, stocksDate, twseDate, tpexDate, chipsDate, sectorDate, signalsDate, indexDate, sectorHistoryLen }`（欄位齊備供 P1-6 用）
  2. `fetch-daily.mjs` Guard 1 改讀 meta.json 判斷；meta.json 404 時 fallback 舊邏輯（過渡相容）
  3. yml 調整順序：先跑一個「guard-check」step（純 node，不需 npm ci），輸出 `SHOULD_RUN`；為 false 直接結束；true 才 npm ci + 完整 pipeline
- **AC**：(a) 被攔截的 run 總時長 < 1 分鐘（Actions UI 確認）；(b) meta.json 可公開讀取且欄位齊全；(c) 正常 run 行為與改造前一致（latest.json 內容結構不變）

### P0-4｜chips 欄位契約文件 `[done]`

- **背景**：chips/{date}.json 是兩專案交界，規格只存在兩邊程式碼裡（見 `Technical Discussion/資料契約-Data-Contract.html`）
- **改動檔案**：新建 `../stock-chips-daily/doc/chips-contract.md`；`../stock-chips-daily/CLAUDE.md` 加指引行；本專案 `AGENTS.md` 加指引行（已完成指引行，見 §5）
- **做法**：契約內容 = 每欄位的名稱／型別／單位／來源 API／就緒時間／缺值語意。欄位清單以 `stock_chips_daily.py` 的 PHASE1_JSON 輸出 + `fetch-daily.mjs` 的 `buildChipsEntry()` 消費欄位交集為準，兩邊不一致處列出並標註
- **AC**：文件涵蓋 buildChipsEntry 消費的全部欄位（foreign_spot/trust_spot/dealer_self/dealer_hedge/inst_total/tx_close/tx_change/basis/fx_tx_oi/fx_tx_chg/retail_mtx_pct/retail_imf_pct/pcr/vix/detail.opt_tr_raw/detail.opt_oi_raw）

### P1-1｜檔案分層（Delta 歸檔 + 衍生檔）`[done]`（latest.json 淘汰除外，見執行紀錄）

- **背景**：見 §1.3。latest.json 10MB 級單一可變大檔 → 配額壓力 + 無法回滾 + 前端載入慢
- **改動檔案**：`scripts/fetch-daily.mjs`、`scripts/write-firebase.mjs`、`lib/fetchSnapshot.ts`、`app/aftermarket/page.tsx`（載入邏輯）、`lib/types.ts`
- **做法**（分兩步交付，可跨 session）：
  1. **寫入側**：pipeline 產出 `daily/{date}.json`（當日 Delta：當日全市場價格列、當日 T86 rows、當日 chips、當日大盤 K 棒、當日全球指數）上傳 Supabase，upload header 設 `cache-control: public, max-age=31536000, immutable`；`market.json` / `stocks-lite.json` 同步產出。closes/dates 歷史併入 ohlc.json（`bars[code]` 加 `c:`、頂層加共用 `dates` 交易日曆陣列，個股缺日用 null 對齊）
  2. **讀取側**：`fetchSnapshot.ts` 改組合 `market.json + stocks-lite.json`；`StockDetailSheet` / K 線既有 lazy 載 ohlc.json 邏輯沿用。全部頁面切換完成前 latest.json 照舊產出（雙寫過渡）
- **AC**：(a) 首頁首屏所需下載量 < 500KB（DevTools Network 驗證）；(b) daily/{date}.json 當日收盤後內容固定不再變；(c) N=250 訊號計算結果與改造前一致（抽 3 個訊號比對）；(d) 個股 K 線功能正常
- **注意**：dates 交易日曆去重共用是瘦身最大來源（每支股票省 250 個字串）

### P1-2｜cron 重排 + 事件驅動補強 `[done]`（依賴 P0-3）

- **背景**：T86 約 16:00–16:30 就緒但首班 cron 18:00；融資 17:00–22:00 靠盲目輪詢。見 `事件驅動-Event-Driven-vs-Polling.html`
- **改動檔案**：`.github/workflows/daily-fetch.yml`；n8n workflow（stock-chips-daily 側）
- **做法**：
  1. cron 改 7 班（總數不增）：台北 15:37 / 16:07 / 17:07 / 18:37 / 20:07 / 22:07 / 23:37（避開整點，GitHub 整點排程易延遲）。UTC 寫法：`37 7`, `7 8`, `7 9`, `37 10`, `7 12`, `7 14`, `37 15`，全部 `* * 1-5`
  2. n8n 補一個「融資就緒 → dispatch」：stock-chips-daily Phase 2（21:00 起）抓到 MI_MARGN 後呼叫 GitHub API `workflow_dispatch`（複用 Phase 1 現有 dispatch 節點的 credential）
  3. Guard 2（FMTQIK 15:30 就緒）不變；15:37 班次即可更新大盤 K 線
- **AC**：(a) 交易日 16:30 前 Supabase 上 sectorHistory 已含當日（比現況提早 ~2hr）；(b) 一週觀察 Actions 用量沒有上升（run 次數 × 平均時長）
- **注意**：必須在 P0-3 完成後做，否則多出的早班會加重 egress

### P1-3｜泡泡圖單位改億元 `[done]`

- **背景**：目前 X/size 用「張」，低價股權重被高估。Tide 用億元，金額才能跨板塊比較
- **改動檔案**：`scripts/fetch-daily.mjs`（`fetchT86Sectors`）、`lib/calcSectors.ts` + `scripts/calc-sectors.mjs`（兩份同邏輯都要改）、`lib/types.ts`、`components/bubble/BubbleChart.tsx`（軸標籤）、`components/bubble/SectorPanel.tsx`（個股列顯示）
- **做法**：T86 分組時股數 × 該股收盤價（stockMap 有 close）→ 億元（`shares * close / 1e8`）。`SectorDayData.rows` 的 net/buySell 與 stocks[].net 等欄位全部改存億元；**sectorHistory 舊資料是張數，需一次性重建**（用 `seed-sectors.mjs` 重抓 25 天，或寫轉換腳本按當日收盤價換算——建議重抓，準確）
- **AC**：(a) 泡泡圖 X 軸單位顯示「億元/日」；(b) 抽一板塊手算驗證：Σ(個股外資淨買股數×收盤價)/1e8 與顯示值一致（±1%）；(c) SectorPanel 個股列的三大法人欄顯示億元
- **注意**：這會改變 sectorHistory 資料格式 → 加 `unit: "yi"` 欄位標記新格式，calcSectors 遇舊格式（無標記）時不混算

### P1-4｜symlog 軸 `[done]`（依賴 P1-3）

- **背景**：板塊資金量級差百倍，線性軸小板塊全擠原點。見 `對數刻度-Symlog-Scale.html`
- **改動檔案**：`components/bubble/BubbleChart.tsx`（`toSVG()` 與軸刻度）
- **做法**：X 軸 symlog：`f(v) = sign(v) * log10(1 + |v|/linthresh)`，`linthresh` 取 1（億元）；刻度標籤仿 Tide：0/±5/±20/±50/±100/±200/±500。Y 軸（加速指標，範圍小）維持線性。trail 座標同步用新映射
- **AC**：(a) 最大板塊與最小非零板塊同時可見且不重疊在原點；(b) 象限縮放（zoom）功能正常；(c) collision resolution 的位移量顯著下降（目測泡泡位置與軸刻度對得上）

### P1-5｜四象限統計條 + 排行榜視圖 `[done]`（依賴 P1-3）

- **背景**：對標 Tide 首屏「漲潮 43｜輪動 9｜觀望 20｜退潮 40｜⚓逆勢買超 N」
- **改動檔案**：`components/bubble/` 新增 `QuadrantSummary.tsx`、`SectorRanking.tsx`；`app/aftermarket/page.tsx`（產業板塊 Tab 加視圖切換）
- **做法**：統計條 = 按象限 count sectors；「逆勢買超」= 大盤當日下跌且板塊淨買超 > 0（大盤漲跌從 indexHistory[0] 取）。排行榜 = 板塊列表按近 5 日均淨買超（億元）排序，顯示 買超額 + 當日板塊平均漲跌%（成分股加權或簡單平均，簡單平均即可）+ 象限色點，點列開啟既有 SectorPanel
- **AC**：(a) 統計條四數字加總 = 板塊總數；(b) 泡泡圖/排行榜切換不重新 fetch；(c) 手機寬度下排行榜單行不溢出

### P1-6｜資料鮮度統一顯示 `[done]`（依賴 P0-3 的 meta 欄位）

- **背景**：各資料就緒時間不同，「頁面更新了但部分是昨天的」是常態。決策見 §1.2 第 7 點
- **改動檔案**：`app/aftermarket/page.tsx`、各區塊 component、`lib/types.ts`（snapshot 加各 date 欄位，pipeline 側在 P0-3 已產出）
- **做法**：頁首顯示「今日 YYYY-MM-DD」（Asia/Taipei）；各區塊（K線/籌碼面板/訊號卡/泡泡圖/個股表）比對自己的 date 戳：= 今日 → 正常；< 今日且今日為交易日 → 角落標「更新中」；今日非交易日 → 頁首顯示「最近交易日 YYYY-MM-DD」且區塊不標更新中。交易日判定：indexHistory[0].date 是否 = 今日，加上「今日是週末」的前端判斷（假日 fallback：18:00 前顯示最近交易日、18:00 後仍無今日資料視為非交易日——避免要維護假日表）
- **AC**：(a) 交易日 16:00 打開頁面：K 線=今日、籌碼可能「更新中」，標示正確；(b) 週六打開：顯示「最近交易日 週五日期」，無任何「更新中」；(c) 個股表頭 twse/tpex 日期分開標示
- **注意**：MOCK_DATA fallback 同步處理——mock 模式時全版面加半透明「示範資料」浮水印（Franky 指出誤看風險）

### P2-1｜概念股分類系統 `[done]`（實際結果與原計劃的差異見下方＋§5 執行紀錄）

- **背景**：對標 Tide 100+ 細分概念板塊，這是泡泡圖價值的核心升級。決策：一股多概念；初版與後續擴充由 AI session 內分析（**不呼叫 OpenAI/任何 LLM API**）；月度維護走 OpenAI + 人工確認（見 P2-6）
- **實際結果（2026-07-09 完成，取代下方原始「後續工作」規劃）**：
  1. Franky 抽驗：seed v1 抽 28 檔知名股（2317鴻海/2382廣達/2308台達電等）全數正確，核准使用，**不需重做**
  2. 覆蓋率擴充：改用「當日 T86 活躍個股」代理指標（而非原規劃的 20 日累積，因架構限制見 §5 2026-07-09 紀錄），另補做一次性 20 日歷史 T86 抵現腳本 `scripts/scan-concept-coverage.mjs`；AI 逐批分類 432 支、新增 6 概念（證券/租賃服務/造紙/建材陶瓷/軌道運輸/物流倉儲），**AC(b) 達標：前800活躍個股覆蓋率 90.0%**（720/800）
  3. pipeline 整合：`fetch-daily.mjs` 的 `fetchT86Sectors` 延伸為同一份 T86 response 同時回傳官方 `sectorRows`/概念 `conceptRows`（不額外呼叫 TWSE）；新增 `conceptHistory`（結構同 sectorHistory）；`calc-sectors.mjs`/`lib/calcSectors.ts` 新增 `calcConcepts`（`matchFn` 參數化與 `calcSectors` 共用全部 X/Y/size/trail 邏輯）；`market.json` 加 `conceptHistory`/`concepts`；`app/aftermarket/page.tsx` 產業板塊 Tab 加「官方分類/概念分類」切換
- **現況**：`data/concept-sectors.json` version `2026-07-09.2`，112 概念、823 支個股
- **原始規劃（僅供對照，AC(b) 20日累積口徑已被上方取代）**：json 格式 `{version, updatedAt, concepts: {名: {desc}}, stocks: {code: [概念...]}}`
- **注意**：概念泡泡圖的 X/size 一股計入多概念會重複計算資金——這是特性不是 bug（Tide 亦同），UI 已加註明文字（`sectorSource==='concept'` 時顯示提示）

### P2-2｜個股概念 tags UI `[done]`（依賴 P2-1）

- **改動檔案**：`components/aftermarket/StockTable.tsx`、`components/stock/StockDetailSheet.tsx`、`components/bubble/SectorPanel.tsx`、新增 `components/shared/ConceptTags.tsx`（三處共用元件，原計劃未列但為避免重複程式碼而新增）
- **做法**：個股列尾 + 詳情頁 + SectorPanel 個股列（原計劃未明訂，此 session 一併補上）顯示概念 tag chips（一股多 tag）；點 tag → 開該概念的 SectorPanel。`fetch-daily.mjs` 直接把 `concepts` 陣列附到每支股票，透過既有的 `...rest` 展開機制自動流進 stocks-lite.json（未特別改 write-firebase.mjs）
- **AC**：台積電顯示 ≥2 個 tag（實測：晶圓代工+先進封裝）；點 tag 正確開啟概念面板；無概念的股票不顯示空 tag 區（`ConceptTags` 元件 `!concepts?.length` 時回傳 null）

### P2-3｜全球指數燈號 + Modal `[done]`（資料源與原計劃不同，詳見下方＋§5 執行紀錄）

- **背景**：決策見 §1.2 第 6 點。日常投資的多市場大盤條件判斷
- **⚠️ 資料源變更（重要，下個 session 讀這段避免走回頭路）**：原計劃用 Stooq CSV，**2026-07-09 實測發現 Stooq 已改成需要瀏覽器算 SHA-256 proof-of-work 才能拿資料**（反爬蟲機制，curl/伺服器端腳本直接拿到的是 JS 挑戰頁不是 CSV，GitHub Actions 也不可能為此裝無頭瀏覽器）。**已改用 Yahoo Finance 非官方 chart API** `https://query1.finance.yahoo.com/v8/finance/chart/{symbol}?range=2y&interval=1d`，免 key，實測 6 個 symbol 全部正常：`^GSPC`(S&P500)、`^DJI`(道瓊)、`^IXIC`(那斯達克)、`^SOX`(費半)、`^N225`(日經225)、`^KS11`(KOSPI)。此決策已跟 Franky 確認（2026-07-09 對話）
- **改動檔案（實際）**：新增獨立 `scripts/fetch-global.mjs`（零 npm 依賴，只用內建 fetch）、`.github/workflows/daily-fetch.yml`（新增台北 06:07 cron，獨立 `fetch-global-indices` job，用 `github.event.schedule` 字串比對切換）、`scripts/write-firebase.mjs`（讀 `global-indices.json` 併入 market.json，不呼叫 Yahoo）、新增 `lib/calcMA.ts`（MA 計算 + 週線重採樣）、新增 `components/aftermarket/GlobalIndexLights.tsx`、`GlobalIndexModal.tsx`、大改 `components/aftermarket/KlineChart.tsx`（加 title prop + MA 疊線 + 日/週切換，全球指數 Modal 直接複用同一顆元件）
- **做法（實際）**：
  1. `fetch-global.mjs` 抓 `range=2y`（~500交易日）但只存最近 **250 天**進 `global-indices.json`（比照台股大盤既有慣例，估 6 指數 +~66KB，避免逼近 P1-1 訂的 500KB 首屏預算；原計劃提的 400 天/3000 天方案沒有採用）。此腳本獨立於主 pipeline，寫入 Supabase Storage 的 `global-indices.json`，`write-firebase.mjs` 下次正常 run 時讀取併入 `market.json.globalIndices`（解耦，避免每個 cron 班次都打 Yahoo）
  2. 燈號邏輯：找價格低於的「最短天期」MA 顯示（MA10→20→60→120 依序檢查），文字「XX 位在 N 日線以下」；全部在均線上 → 顯示「全數位在 120 日線之上」；紅點=偏多（均線之上）、綠點=偏空（均線之下），對應台股紅漲綠跌慣例
  3. **週線 MA120 拿掉**：只存 250 天 ≈ 50 週，不夠算 MA120（需 120 週回看）。週線只提供 MA10/20/60，日線維持完整 MA10/20/60/120（`KlineChart.tsx` 的 `MA_LINES` 用 `dailyOnly` 標記）
  4. **MA 前端算**（比照專案既有「N 值前端計算」慣例）：pipeline 只存原始 OHLC，MA 由 `lib/calcMA.ts` 前端計算，台股大盤 K 線與全球指數 Modal 共用同一套算法
  5. 資料進 market.json 的 `globalIndices` 區；美股資料日期 = 台北時間昨日（已實測確認：美股顯示 2026-07-08、亞股顯示 2026-07-09，正確反映各市場自己的交易日曆），鮮度標示在 Modal 內顯示「資料至 {date}」
- **AC**：(a) 首頁燈號區只有文字+色點，不佔超過兩行高度 —— 已用 flex-wrap 排版；(b) Modal 日/週切換正常、MA 線正確 —— 邏輯已 node 單測驗證，與外部網站比對待部署後人工抽驗；(c) 台股 K 線的 MA/週線功能同步可用 —— KlineChart.tsx 同一元件兩處共用；(d) 06:07 班次只跑指數更新，< 1 分鐘結束 —— 獨立 job 不裝 npm 依賴，理論上符合，實際耗時待首次排程觸發後於 Actions UI 確認
- **注意**：Yahoo Finance 為非官方端點，symbol 或回應格式若跑掉會悄悄失敗（`fetch-global.mjs` 有 try/catch 個別指數保留舊值，不會讓整個 run 失敗，但需留意 log）

### P2-4｜AI 盤後總結 + LINE 推播 `[todo]`（依賴 P1-1 穩定）

- **背景**：對標 Tide「今日重點」（情緒、避風港板塊、昨日買超板塊今日表現回顧、大戶異常、AI 總結）。Franky 已有 n8n + LINE Bot 基建（見 memory `project_line_bot_group_manager`）
- **做法**：
  1. pipeline 產出結構化事實 `daily-brief-facts.json`：四象限統計、逆勢買超板塊、昨日 top3 買超板塊的今日平均漲跌、個股異常（單日淨買/賣超金額 > 閾值，建議 |淨買超| > 30 億）
  2. n8n 排程（pipeline 完成後）：讀 facts → **OpenAI API**（gpt-4o-mini，繁中 prompt，150 字內盤後總結）→ LINE push 給 Franky
  3. 網頁端：market.json 加 `dailyBrief` 區塊，首頁頂部可折疊卡片顯示
- **AC**：(a) LINE 訊息含：總結文字 + 逆勢買超板塊 + 異常個股清單；(b) OpenAI 失敗時仍推送純事實版（AI 文字非阻斷）；(c) 月成本 < USD 1
- **注意**：情緒分數第一版用簡單公式（如：漲潮板塊數/總數 + 大盤漲跌 z-score 合成），不要過度設計

### P2-5｜觀察清單 + 個股泡泡回放 `[done]`（架構與原計劃有落差，詳見下方＋§5 執行紀錄）

- **⚠️ 資料架構阻礙（重要，下個 session 讀這段）**：原計劃寫「sectorHistory rows.stocks 已有」個股歷史資料，**這句話錯了**——P1-1 瘦身時 `sectorHistory`/`conceptHistory` 只有 day0（今天）保留 `rows[].stocks` 個股明細，過去 24 天只剩板塊層級加總數字，個股層級的歷史淨買超完全不存在於 `market.json`。2026-07-09 跟 Franky 確認後，新增獨立 lazy-load 檔 `stock-history.json`（比照 `ohlc.json` 模式）解決：pipeline 每天照既有排程自動維護 25 天滾動個股歷史（**不是一次性腳本**，`fetch-daily.mjs` 的 `fetchT86Sectors` 已經在算 T86 個股明細，只是多存一份扁平清單），前端只在使用者實際打開「⭐ 自選股」模式時才 fetch 這個檔案（不影響首屏 500KB 預算）
- **做法（實際）**：觀察清單 `useWatchlist()` hook（`lib/watchlist.ts`，localStorage 免登入，CustomEvent 同步同分頁狀態）；個股表（`StockTable.tsx`）+ 詳情頁（`StockDetailSheet.tsx`）加 ⭐ 切換按鈕；泡泡圖新增「⭐ 自選股」資料源（`app/aftermarket/page.tsx` 的 `sectorSource` 第三選項），`lib/calcWatchlistBubbles.ts` 算法與 `calcSectors` 完全相同公式，只差分組單位（單一個股 vs 預聚合板塊/概念）
- **Ghost 陪跑（2026-07-09 補做完成）**：回放時同概念股以半透明泡泡陪跑，設計取捨：只取聚焦股票的「第一個」概念（一股多概念時的簡化）、依當日活動量排序取前 8 支、資料缺值不特別處理（沿用 calcWatchlistBubbles 既有的 0 值 fallback，rolling average 已自然平滑，未做「維持上次位置」的複雜化）。`concept-sectors.json` 走動態 import，只在自選股模式+已聚焦時才載入，不影響一般使用者的 JS bundle
- **注意**：2026-07-09 已做「板塊聚焦回放」（BubbleChart 的 focused/replayStep/focusedPath overlay），個股回放直接複用同一套 overlay 機制（`calcWatchlistBubbles` 輸出的 `SectorBubble[]` 形狀與板塊/概念完全一致，BubbleChart 元件本身完全沒改）
- **AC**：(a) 加入/移除即時反映 —— `useWatchlist` 的 CustomEvent 機制達成；(b) 個股回放軌跡與其板塊面板數字一致 —— SectorPanel 顯示的 `sector.stocks[0]` 與泡泡的 x/y 來自同一次 `calcWatchlistBubbles` 呼叫，數學上保證一致；(c) 清空 localStorage 後功能仍正常（空狀態引導）—— `watchlistCodes.length === 0` 時顯示引導文字，不渲染空泡泡圖

### P3-1｜盤中即時 `[deferred]`

- Fugle WebSocket（key 已在 .env.local 與 GitHub Secrets，見 memory reference_tech_notes「Fugle」條目）。**明確延後，其他全部完成前不做**。定位：盤中量價 proxy（非法人資料），UI 需明確標示

### P2-6｜概念分類月度維護自動化 `[todo]`（依賴 P2-1 覆蓋率達標）

- **做法**：n8n 每月排程：掃 concept-sectors.json 未覆蓋但 T86 活躍的個股 → OpenAI 建議分類 → 產 diff → LINE 推 Franky 確認 → 確認後寫回 json + git commit
- **AC**：全流程只需 Franky 按一次確認；未確認不改正本

---

## 4. 進度總覽

| ID | 任務 | 優先 | 狀態 | 依賴 |
|---|---|---|---|---|
| P0-1 | concurrency group 修正 | P0 | `done` | — |
| P0-2 | buildChipsEntry null crash | P0 | `done` | — |
| P0-3 | Guard 前置 + meta.json | P0 | `done` | — |
| P0-4 | chips-contract.md | P0 | `done` | — |
| P1-1 | 檔案分層 Delta+衍生檔 | P1 | `done`（雙寫過渡中，AC(a) 量測待部署後） | P0-3 |
| P1-2 | cron 重排+事件驅動 | P1 | `done` | P0-3 |
| P1-3 | 泡泡圖單位改億元 | P1 | `done` | — |
| P1-4 | symlog 軸 | P1 | `done`（視覺驗證待部署後） | P1-3 |
| P1-5 | 統計條+排行榜 | P1 | `done`（視覺驗證待部署後） | P1-3 |
| P1-6 | 資料鮮度統一 | P1 | `done`（tpex 拆分遺留） | P0-3 |
| P2-1 | 概念股分類系統 | P2 | `done`（抽驗+覆蓋率擴充+pipeline整合皆完成，視覺驗證待部署後） | — |
| P2-2 | 個股概念 tags | P2 | `done`（視覺驗證待部署後） | P2-1 |
| P2-3 | 全球指數燈號+Modal | P2 | `done`（視覺驗證待部署後） | — |
| P2-4 | AI 盤後總結+LINE | P2 | `todo` | P1-1 |
| P2-5 | 觀察清單+個股回放 | P2 | `done`（含Ghost陪跑，視覺驗證待部署後） | P2-1 |
| P2-6 | 概念月度維護自動化 | P2 | `todo` | P2-1 |
| P3-1 | 盤中即時 | P3 | `deferred` | 全部 |

**建議執行順序**：P0-1 → P0-2 → P0-3 → P0-4 → P1-3 → P1-4 → P1-5 → P1-1 → P1-2 → P1-6 → P2-1 續 → P2-2 → P2-3 → P2-5 → P2-4 → P2-6

---

## 5. 執行紀錄（append-only，格式：日期｜任務ID｜內容｜驗證）

- 2026-07-08｜計劃建立｜本文件 + `data/concept-sectors.json` seed + Technical Discussion 7 份文件 + AGENTS.md 指引行｜—
- 2026-07-08｜P0-1｜yml concurrency group 改固定字串 daily-fetch，註解更新｜排隊行為需 push 後在 Actions UI 驗證（連續兩次 dispatch）
- 2026-07-08｜P0-2｜fetch-daily.mjs buildChipsEntry 的 pcr/vix 補 ?.｜單測通過：buildChipsEntry(null,123.45,120) 回傳 partial entry、全 null 回傳 null、正常路徑 OK；AC 已寫入 Notion Spec（39592ca3）
- 2026-07-08｜P0-3｜新增 scripts/guard-check.mjs（零依賴前置 Guard）；write-firebase.mjs 產出 meta.json；fetch-daily.mjs Guard1 先讀 meta.json（404 fallback latest.json）；yml：guard 前置、npm ci/firebase-admin/fetch 全部 gate 在 SHOULD_RUN=true｜本機驗證：交易日→SHOULD_RUN=true、DATE_OVERRIDE=週日→false；meta.json 首次上傳需等下次成功 run
- 2026-07-08｜P0-4｜契約正本建於 ../stock-chips-daily/doc/chips-contract.md（上游慣例 doc/）；上游新建 CLAUDE.md 指引；本專案 AGENTS.md 指引已修正路徑。發現並記錄：n8n 整包上傳 PHASE1_JSON、檔名日期依賴 TAIFEX 西元格式｜欄位覆蓋 buildChipsEntry 全部消費欄位
- 2026-07-08｜P1-3｜fetchT86Sectors 股數→億元（該日收盤價從個股歷史查表，缺則用最新 close 近似；加 dateISO 參數）；sectorHistory 加 unit:'yi' 標記，載入時 filter 掉舊「張」格式→5b 回補機制自動用新單位重抓 25 天；calcSectors ts+mjs 移除 /1000；types.ts 註解與 unit 欄位；SectorPanel 標籤(張)→(億)、數字 toFixed(2)、header 千張→億/日；BubbleChart 軸標籤加（億/日）｜mock T86 單測：外資 1,000,000 股×1000 元=10 億 ✅、淨賣 -1 億 ✅；tsc 通過。注意：部署後首次 run 會重抓 25 天 T86（~40 秒延遲，單次性）
- 2026-07-08｜P1-4｜BubbleChart 加 symX() symlog 轉換（linthresh=1 億）：xRange/泡泡座標/trail 全走 symlog 空間，Y 軸維持線性；加 Tide 式刻度標籤 ±5/±20/±50/±100/±200/±500（僅渲染落在範圍內的）｜tsc 通過；泡泡分散度與 collision 位移下降需部署後視覺驗證
- 2026-07-08｜P1-5｜新增 QuadrantSummary.tsx（四象限計數 + ⚓逆勢買超：大盤跌且板塊當日淨買超>0）、SectorRanking.tsx（近5日均買賣超排序 + 橫條 + 當日板塊平均漲跌%，點列開 SectorPanel）；page.tsx 產業板塊 Tab 加泡泡圖/排行榜切換｜tsc 通過；本機 next build 因 SWC binary 缺失無法跑（環境問題，Vercel CI 正常），視覺驗證待部署後
- 2026-07-08｜備註｜stock-dashboard repo 確認為 PUBLIC → GitHub Actions 分鐘數無上限，§0.3 的 Actions 約束只在轉 private 時才生效；Supabase egress 約束不變
- 2026-07-09｜P1-1｜寫入側：write-firebase.mjs 新增 market.json（sectorHistory 僅 day0 保留個股）、stocks-lite.json、history.json（closes 對齊共用交易日曆、缺日 null）、daily/{date}.json Delta（prices 僅在 stocksDate=當日時寫入；cache 暫設 max-age=300，收斂期不能 immutable）。讀取側：fetchSnapshot 改組合三檔還原 SnapshotData 原形狀（下游零改動），任一檔 404 fallback latest.json（部署順序保護）。latest.json/ohlc.json 雙寫照舊｜合成 snapshot 單測通過（day0/day1 剝除、null 對齊、delta 內容）；tsc 通過
- 2026-07-09｜P1-1 計劃偏差註記｜closes 歷史改放獨立 history.json 而非併入 ohlc.json —— ohlc 的 o/h/l/v 對齊各股自己的 dates，history 對齊共用日曆，混在一檔會有兩種對齊方式易出錯。AC(a) 首屏 <500KB 待部署後 DevTools 實測：history.json ~3MB（gz 後估 ~700KB）可能超標，若超標下一步是二段式載入（market+lite 先渲染、history 背景補）——接續 session 注意
- 2026-07-09｜P1-1 遺留｜latest.json 淘汰：等分層檔案跑穩一週後，移除 write-firebase 的 latest.json 上傳與 fetchSnapshot 的 fallback，屆時 Guard 1 的 fallback 路徑一併清理
- 2026-07-09｜P1-2｜yml cron 重排為台北 15:37/16:07/17:07/18:37/20:07/22:07/23:37（7 班不增，避開整點）；n8n Phase 2（oIAVOsEYKmtb5ePI）成功路徑（解析摘要 JSON 後）加「觸發儀表板更新」dispatch 節點，複用 Phase 1 的 GitHub PAT credential（dMaQFTkWrG5nIyk6），PUT + deactivate/activate 完成，git JSON 已同步｜AC(a) 交易日 16:30 前 sectorHistory 含當日：待下一交易日驗證；AC(b) Actions 用量觀察一週
- 2026-07-09｜P1-6｜新增 components/shared/FreshnessBar.tsx：頁首「今日 MM/DD」+ 五個資料塊狀態（K線/籌碼/融資/板塊/股價：✓ 或 更新中）；非交易日顯示「最近交易日」不標更新中；假日判定 = 平日 18:00 後仍無今日 K 棒（不維護假日表）。日期戳直接從快照內容推導（layered 與 fallback 都適用；market.json 同步帶 server 端戳供 meta 一致性）。Mock 模式加全版面「示範資料·非真實行情」浮水印｜tsc 通過
- 2026-07-09｜P1-6 範圍註記｜(1) twse/tpex 股價日期拆分未做——pipeline 目前只有單一 stocksDate，拆分需 fetch-daily 記 tpexDate，列為後續小任務；(2)「倒退看前一日」的 UI 屬 daily/{date}.json 的讀取功能，另列（資料基礎 P1-1 已備）；(3) 頁首右上原有的 updatedAt 時間標籤保留（與 FreshnessBar 並存，不衝突）
- 2026-07-09｜回放重設計（Franky 指示，計劃外）｜泡泡圖首頁移除常駐軌跡線與頂部回放列（全圖 frames 回放整組移除，page.tsx 不再算 frames）；改 Tide 式聚焦回放：點泡泡→其他泡泡淡出+聚焦小卡（▶回放/個股清單/✕），按回放才漸進畫軌跡+泡泡沿 trail(≤5點)+今日移動，小卡顯示回放日期；個股清單改由小卡按鈕開啟 SectorPanel｜tsc 通過；本機 dev 因 SWC binary 缺失不能跑，視覺驗證待 Vercel 部署後；AC-RP-1~4 已寫入 Notion Spec
- 2026-07-09｜P2-1 續①②｜人工抽驗：抽 28 檔知名股（2317鴻海/2382廣達/2308台達電等）分類全數正確，seed v1 核准使用。覆蓋率擴充：新增 `scripts/scan-concept-coverage.mjs`（一次性腳本，不進 pipeline）用近20日 T86 活動量排序找未分類個股（market.json 尚未產生，fallback 讀 latest.json 取交易日曆）；AI 逐批分類 432 支（420+二輪補12），新增 6 概念（證券/租賃服務/造紙/建材陶瓷/軌道運輸/物流倉儲）｜AC(b) 達標：前800活躍個股覆蓋率 90.0%（720/800，精確達標）；隨機抽 20 支二次驗證全數正確；version bump 至 2026-07-09.2，共112概念/823支個股
- 2026-07-09｜P2-1 遺留｜~345 支低活動量個股仍未分類（超出前800名門檻，非 AC 要求範圍）；`data/concept-coverage-scan.json` 保留完整掃描結果供未來擴充參考
- 2026-07-09｜P2-1 續③｜pipeline 整合完成：`fetchT86Sectors` 延伸為同一份 T86 response 同時回傳 `sectorRows`/`conceptRows`（不額外呼叫 TWSE）；新增 `conceptHistory`（結構同 sectorHistory，25天滾動）；`calc-sectors.mjs`/`lib/calcSectors.ts` 的 `calcSectors` 加 `matchFn` 參數化，新增 `calcConcepts` 共用同一套 X/Y/size/trail 邏輯；`write-firebase.mjs` market.json 加 `conceptHistory`（day0 才留 stocks，同 sectorHistory 規則）+ `concepts`；`fetchSnapshot.ts` 還原時一併帶出；`app/aftermarket/page.tsx` 產業板塊 Tab 新增「官方分類/概念分類」切換（`sectorSource` state），activeSectors/activeSectorHistory 同時餵給 QuadrantSummary/BubbleChart/SectorRanking/frameDates；概念模式下顯示「一股可能屬於多個概念，資金會重複計算」提示；`lib/mockData.ts`/`lib/types.ts` 同步加欄位｜tsc 通過；node 單測驗證：同一股同時計入 2 個概念時兩邊數值一致且與官方分類金額換算一致（1,000,000股×100元/1e8=1億）；regression-map.json 已補 6 條新/改動函式。視覺驗證（切換不重新 fetch、泡泡圖正確顯示）待 Vercel 部署後
- 2026-07-09｜P2-1 待辦（Notion）｜本 session 無 Notion MCP 連線，AC(c) 補充草稿已在對話中列出但未寫入 Spec（ID 39592ca3-5773-81b8-87cf-e5614fcdea87），下次有 Notion 連線的 session 需手動補寫
- 2026-07-09｜P2-2｜個股概念 tags UI 完成：`fetch-daily.mjs` 在 `stocks` 陣列組好後附上 `s.concepts = conceptStockMap[s.code] ?? []`（透過 stocks-lite.json 的 `...rest` 展開自動流通，未特別改 write-firebase.mjs）；`lib/types.ts` StockData 加 `concepts?: string[]`；新增共用元件 `components/shared/ConceptTags.tsx`（無概念回傳 null，點擊 stopPropagation 避免觸發外層列點擊）；`StockTable.tsx`/`StockDetailSheet.tsx`/`SectorPanel.tsx` 三處都加了概念欄位/區塊；`page.tsx` 新增 `handleConceptClick`（從 `data.concepts` 找對應 SectorBubble 開啟 SectorPanel，並關閉個股詳情頁）｜tsc 通過；台積電(2330)資料含 2 概念（晶圓代工/先進封裝）符合 AC；視覺驗證（tag 顯示、點擊開面板）待部署後
- 2026-07-09｜P2-2 待辦（Notion）｜同上，AC 草稿需補寫：(a) StockTable/StockDetailSheet 顯示 concepts tag chips，無概念不顯示空區；(b) 點 tag 開啟對應概念 SectorPanel；(c) SectorPanel 個股列同步顯示概念 tags（計劃原文未明訂但改動檔案清單已含 SectorPanel.tsx，此 session 一併完成）
- 2026-07-09｜P2-3 計劃偏差｜Stooq CSV 端點已改成需瀏覽器解 JS proof-of-work（SHA-256 挑戰）才能拿資料，伺服器端腳本無法解，實測直接被擋（curl 回傳 JS 挑戰頁而非 CSV）。改用 Yahoo Finance 非官方 chart API（`query1.finance.yahoo.com/v8/finance/chart/{symbol}`），實測 6 個 symbol 全部正常：`^GSPC`(S&P500)/`^DJI`(道瓊)/`^IXIC`(那斯達克)/`^SOX`(費半)/`^N225`(日經225)/`^KS11`(KOSPI)，回傳含各市場自己時區的 OHLC+timestamp，免 key。Franky 已確認此改動｜此決策記錄供未來 session 不要走回頭路試 Stooq
- 2026-07-09｜P2-3 儲存決策｜抓 range=2y（~500交易日）但只存最近 250 天進 market.json（比照台股大盤慣例，估 6 指數 +~66KB，避免逼近 P1-1 的 500KB 首屏預算）；週線 MA120 因此拿掉（250天≈50週不夠算120週MA），週線只提供 MA10/20/60，日線維持完整 MA10/20/60/120；MA 前端算（比照現有 N 值前端計算慣例），pipeline 只存原始 OHLC
- 2026-07-09｜P2-3 完成｜新增 `scripts/fetch-global.mjs`（獨立輕量腳本，零 npm 依賴、只用內建 fetch，讀舊值→逐指數抓→個別失敗保留舊值→整包上傳 Supabase `global-indices.json`，不碰 latest.json/主 pipeline）；`.github/workflows/daily-fetch.yml` 加台北06:07 cron（UTC `7 22 * * 0-4`，因跨日所以星期用0-4對應台北一~五），新增 `fetch-global-indices` job 用 `github.event.schedule` 字串比對只在該班跑，`fetch-and-upload` job 加排除條件避免同班重複跑兩個 job；`scripts/write-firebase.mjs` 讀 `global-indices.json` 併入 `market.json.globalIndices`（不呼叫 Yahoo，避免每個 cron 班次重複打）；新增 `lib/calcMA.ts`（calcMA + resampleWeekly，ISO週週一為週首）；`components/aftermarket/KlineChart.tsx` 大改：加 title prop（供全球指數 Modal 複用）、日/週切換、MA10/20/60(/120限定日線)疊線（lookback不足處斷線不強畫）、pMin/pMax 範圍納入MA值避免裁切；新增 `GlobalIndexLights.tsx`（燈號：找價格低於的最短天期MA，紅=偏多/綠=偏空對應台股慣例）、`GlobalIndexModal.tsx`（複用KlineChart）；`page.tsx`/`lib/types.ts`/`lib/fetchSnapshot.ts` 三處接線｜tsc 全過；node 單測驗證 calcMA/resampleWeekly 算法正確（MA5手算比對、週線OHLC合併正確）；**已實際執行 fetch-global.mjs 對生產 Supabase 寫入 global-indices.json 驗證端到端**：6指數全部成功，日期符合預期（美股2026-07-08/亞股2026-07-09，反映各市場時區差異）；視覺驗證（燈號顯示、Modal圖表、日週切換）待下次 write-firebase.mjs 正常 run（今天15:37起）+ Vercel 部署後
- 2026-07-09｜P2-3 待辦（Notion）｜AC 草稿需補寫：(a)首頁燈號區只有文字+色點（已用flex-wrap兩行內）；(b) Modal日/週切換正常、MA線正確；(c) 台股K線MA/週線同步可用；(d) 06:07班次獨立job不裝npm依賴，預期<1分鐘
- 2026-07-08｜備註（已解決）｜(1) tests/regression-map.json 已建立（16 個入口，含 pipeline 腳本與泡泡圖元件）；(2) n8n 硬編碼 Supabase key 已修：SUPABASE_SERVICE_KEY 加入 com.n8n.local.plist → n8n 重啟 → Phase1 workflow（h81CNqkowf1qQ9dK）經 API 改用 $env 讀取（PUT + deactivate/activate）→ 兩個 git JSON 已同步，全專案掃描無殘留。⚠️ 待辦：key 已存在 git 歷史，真正除險需 Franky 到 Supabase Dashboard rotate service key，然後更新 GitHub Secrets 的 SUPABASE_SERVICE_KEY + n8n plist 兩處。⚠️ $env 讀取路徑明日 15:30 Phase1 首跑才算實證，若 chips/{date}.json 沒產生要先查這裡

- 2026-07-09｜P2-5 計劃偏差｜原計劃寫「泡泡圖自選股模式用 sectorHistory rows.stocks 已有的個股資料」——**此假設錯誤**，P1-1 瘦身後 sectorHistory/conceptHistory 只有 day0 保留個股明細，過去24天沒有個股層級歷史。跟 Franky 確認後採用「新增 lazy-load 個股歷史檔」方案（比照 ohlc.json 模式，非一次性腳本，pipeline 每天自動累積 25 天滾動視窗，前端只在開自選股模式才 fetch）
- 2026-07-09｜P2-5 完成｜Pipeline：`fetchT86Sectors` 再擴充回傳 `stockRows`（當日全部 T86 活躍個股扁平清單，含 buySell）；新增 `downloadStockHistory()`/`stockHistory` 變數，5a/5b 兩處都與 sectorHistory/conceptHistory 同步累積 25 天滾動；`write-firebase.mjs` 新增獨立輸出 `stock-history.json`（故意不併入 market.json）。前端：新增 `lib/fetchStockHistory.ts`（lazy fetch + module cache，比照 fetchStockOHLC.ts）、`lib/calcWatchlistBubbles.ts`（與 calcSectors 同公式，分組單位改個股）、`lib/watchlist.ts`（localStorage + useWatchlist hook，CustomEvent 同步）；`StockTable.tsx`/`StockDetailSheet.tsx` 加 ⭐ 切換；`page.tsx` 的 `sectorSource` 加第三選項「⭐ 自選股」，QuadrantSummary 在此模式下跳過不顯示，空清單顯示引導文字｜tsc 全過；node 單測驗證 calcWatchlistBubbles 邏輯（趨勢股正確算出 avg5/accel，無資料股安全回傳 0 不崩潰）
- 2026-07-09｜P2-5 Ghost 補做完成｜跟 Franky 說明技術細節（同族群怎麼選、資料哪裡來、需要改 BubbleChart 核心回放邏輯）後確認照建議預設值做。實作：`lib/calcWatchlistBubbles.ts` 新增 `getGhostPeers(focusedCode, conceptStockMap, stockHistory, cap=8)`（只取第一個概念、依當日 |net|+|buySell| 排序取前8支）；`BubbleChart.tsx` 新增 `onFocusChange`/`ghostBubbles` props（聚焦泡泡改變時用 useEffect 通知外部；ghost 用同一套 toSVG/symX 映射疊在主角軌跡下方、半透明不可互動、同步 replayStep）；`page.tsx` 新增 `conceptStockMap` 動態 import `@/data/concept-sectors.json`（只在自選股模式+已聚焦時才載入，避免影響一般使用者 bundle 大小）+ `ghostBubbles` useMemo｜tsc 全過；node 單測驗證 getGhostPeers 正確排除非同概念股、正確依活動量排序
- 2026-07-09｜P2-5 待辦（Notion）｜AC 草稿需補寫，見下方 §6
- 2026-07-09｜/save｜P2-1/P2-2/P2-3 一批（22 檔案）commit `4a6617c` 已 push；P2-5 一批（11 檔案）commit `1c4a9e0` 已 push。兩次都是 stock-dashboard 獨立 repo（非主 Claude Code repo），main branch 最新狀態即這兩個 commit

---

## 6. 待寫入 Notion Spec（下個有 Notion MCP 連線的 session 處理）

> Notion Spec 文件：Claude|Spec|台股儀表板，ID `39592ca3-5773-81b8-87cf-e5614fcdea87`
> 這幾個 session（2026-07-09）都沒有 Notion MCP 連線（工具沒註冊進 session，重開新 session 才會有），下方 AC 草稿都還只存在這份計劃書，**尚未寫進 Notion**。程式碼與功能都已完成、tsc 通過，純粹是 Notion 文件同步落後，寫入時直接複製下方文字即可，不需要再重新設計 AC。

### P2-1（概念股分類系統）AC 補充
- (a) json 格式：`{version, updatedAt, concepts: {名: {desc}}, stocks: {code: [概念...]}}` —— 已符合，version `2026-07-09.2`
- (b) 覆蓋 T86 近期活躍個股（依活動量排序）前 800 支的 90% 以上 —— **已達標 90.0%（720/800）**。註：口徑從原計劃的「20 日累積」改為「當日活躍代理指標+一次性20日歷史抵現腳本」，原因見上方 P2-1 區塊與 §5 執行紀錄
- (c) `conceptHistory` 泡泡圖可切換「官方分類/概念分類」—— 已完成，`app/aftermarket/page.tsx` 的 `sectorSource` 切換

### P2-2（個股概念 tags UI）AC
- (a) 個股列表/詳情頁/SectorPanel 三處顯示 concepts tag chips，無概念的股票不顯示空 tag 區
- (b) 點 tag 正確開啟對應概念的 SectorPanel
- (c) 台積電(2330) 顯示 ≥2 個 tag（實測：晶圓代工、先進封裝）

### P2-3（全球指數燈號 + Modal）AC
- (a) 首頁燈號區只有文字+色點，不佔超過兩行高度（flex-wrap 排版）
- (b) Modal 日/週切換正常、MA 線正確（邏輯已 node 單測驗證；與外部網站數字比對屬人工抽驗，待部署後做）
- (c) 台股大盤 K 線的 MA/週線功能同步可用（`KlineChart.tsx` 為台股與全球指數共用同一元件）
- (d) 06:07 班次只跑全球指數更新、獨立 job 不裝 npm 依賴，預期 <1 分鐘（實際耗時待首次排程觸發後於 GitHub Actions UI 確認）
- **⚠️ 額外要在 Notion 記一筆資料源變更**：原計劃寫 Stooq，實際改用 Yahoo Finance chart API（非官方端點），原因是 Stooq 加了 JS 反爬蟲機制。若 Notion Spec 內文有寫死 Stooq 字樣，需要一併修正，避免下次有人照 Notion 內容去查 Stooq

### P2-5（觀察清單 + 個股泡泡回放）AC
- (a) 加入/移除觀察清單即時反映在泡泡圖與個股列表的 ⭐ 狀態
- (b) 個股泡泡回放軌跡與其面板（SectorPanel 顯示的單股數字）一致（同一次 `calcWatchlistBubbles` 計算，數學上保證）
- (c) 清空 localStorage 觀察清單後功能仍正常，顯示空狀態引導文字（不是空白泡泡圖）
- **⚠️ 額外要在 Notion 記一筆架構修正**：原計劃寫「個股歷史資料 sectorHistory rows.stocks 已有」，這個假設是錯的（P1-1 瘦身後只有 day0 保留個股明細）。實際新增了獨立 `stock-history.json`（lazy-load，pipeline 每天自動累積 25 天滾動）才解決。若 Notion Spec 內文照抄了這句錯誤假設，需要一併修正
- (d) 回放時同概念股以半透明泡泡陪跑（Ghost，2026-07-09 補做完成）：只取聚焦股票第一個概念、依當日活動量排序取前 8 支陪跑
